# Navigation fix

The primary navigation is fixed to the viewport on desktop and mobile so it remains visible while scrolling. The mobile menu remains anchored to the fixed navigation and retains its accessible toggle behavior.
